import React from 'react';

const WhatsAppWidget = ({ phoneNumber, message }:{phoneNumber:any,message:any}) => {
    return (
        <div>
          <WhatsAppWidget phoneNumber="" message="" />
        </div>
    );
}

export default WhatsAppWidget;
